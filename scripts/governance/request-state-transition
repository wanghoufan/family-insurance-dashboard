#!/usr/bin/env python3
"""
request-state-transition｜V2.3.1 FAIL-CLOSED TEMPLATE

Task Manager may submit a transition request; it must not directly write authoritative state.

This template intentionally DOES NOT perform production actions.
Implement the local trusted adapter, run Contract Tests / Negative Canary,
then update the authoritative registries. Until then it must fail closed.
"""
import sys
print("BLOCKED: request-state-transition is an UNVERIFIED V2.3.1 interface template. Implement and contract-test the local adapter before production use.", file=sys.stderr)
sys.exit(78)
