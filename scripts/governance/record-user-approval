#!/usr/bin/env python3
"""
record-user-approval｜V2.3.1 FAIL-CLOSED TEMPLATE

Must be bound to a trusted user interaction boundary. Task Manager cannot self-report trusted_user_origin.

This template intentionally DOES NOT perform production actions.
Implement the local trusted adapter, run Contract Tests / Negative Canary,
then update the authoritative registries. Until then it must fail closed.
"""
import sys
print("BLOCKED: record-user-approval is an UNVERIFIED V2.3.1 interface template. Implement and contract-test the local adapter before production use.", file=sys.stderr)
sys.exit(78)
