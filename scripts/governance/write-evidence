#!/usr/bin/env python3
"""
write-evidence｜V2.3.1 FAIL-CLOSED TEMPLATE

Must derive producer identity from trusted dispatch/runtime metadata; Task Manager direct use is DENY; append-only, concurrency-safe, duplicate protected.

This template intentionally DOES NOT perform production actions.
Implement the local trusted adapter, run Contract Tests / Negative Canary,
then update the authoritative registries. Until then it must fail closed.
"""
import sys
print("BLOCKED: write-evidence is an UNVERIFIED V2.3.1 interface template. Implement and contract-test the local adapter before production use.", file=sys.stderr)
sys.exit(78)
