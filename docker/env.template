# 部署环境变量模板（仅变量名、说明与占位符，绝不填真实值）
# 适用规范：《Mac Mini 本地项目自托管 Docker 规范 V1.1》
#
# 使用方式：
#   1. 正式部署副本 = /Users/zzymima0000/Developer/coding/docker/family-insurance-dashboard/
#      （该目录为业务仓库的 git 克隆，由 docker/deploy.sh 同步，不是开发目录）
#   2. 复制本模板为部署副本根目录下的 .env.local 后填写；.env.local 不进 Git、不进镜像。
#   3. deploy.sh 用独立配置，避免与其他项目共用同一 .deploy.env：
#      DEPLOY_CONFIG_FILE=/Users/zzymima0000/Developer/coding/docker/.deploy.family-insurance-dashboard.env \
#        /Users/zzymima0000/Developer/coding/docker/deploy.sh https://github.com/wanghoufan/family-insurance-dashboard.git

# ---------- 运行标识 ----------
# 与 Services / DockerData / DockerBackups 目录名完全一致，改名需走迁移计划
PROJECT_SLUG=family-insurance-dashboard
COMPOSE_PROJECT_NAME=family-insurance-dashboard

# ---------- 网络 ----------
# 宿主机监听端口；与 3100(prompt-manager)、3001(分镜生成器)、8000(开发预览) 不冲突
APP_PORT=3200
# 用户访问的完整 URL；变更时须一并评估 Supabase Auth Site URL / Redirect URL
PUBLIC_APP_URL=http://localhost:3200

# ---------- 版本与回滚 ----------
# 每次正式部署必须更新为可识别版本（日期或 Git 短 SHA），禁止长期停在 latest
IMAGE_TAG=family-insurance-dashboard:2026-09-05

# ---------- 目录（部署侧填写，容器暂未使用） ----------
# APP_DATA_DIR=/Users/zzymima0000/DockerData/family-insurance-dashboard
# APP_BACKUP_DIR=/Users/zzymima0000/DockerBackups/family-insurance-dashboard

# ---------- Supabase（接入后启用；浏览器侧只允许公开变量） ----------
# NEXT_PUBLIC_SUPABASE_URL=<占位：云端项目 URL>
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<占位：publishable key>
# 禁止写入本文件、禁止进镜像/Git/日志/截图：service_role、数据库密码、MCP 原始令牌
