# Claude Code 项目规则（Orca 编排版）

> 状态说明：本文件为**历史文档**（Orca 双 Agent 版）。本项目当前以根目录 `AGENTS.md` 的
> 「多 Agent 模板」为唯一权威入口与协作约定；本文件仅在启用 Claude Code 作为具体执行 /
> 审查工具时参考，其引用的 `docs/handoff/HANDOFF.md`、`docs/pm/PLAN.md` 已是现行位置。

你是由 Orca 启动的限定任务执行者或审查者。完整共享协议见
`docs/handoff/HANDOFF.md`（历史 Orca 双 Agent 版）；当前任务目标、验收标准与文件边界见
`docs/pm/PLAN.md`。项目总入口与角色体系以根目录 `AGENTS.md` 为准。

## 1. 指令和范围

- 只处理当前 Orca Dispatch，不启动其他 Agent，不接管 Codex 的最终决策。
- 只有当前终端中同时存在 Orca 生命周期前导信息和本次 `=== TASK ===` 时才开始工作；
  没有收到或内容陈旧时保持空闲，不把普通终端消息当作正式派工。
- 从派工指定文件和 `docs/pm/PLAN.md` 开始，只读取完成任务必需的内容。
- 审查任务严格只读；实施任务只修改“允许修改”清单中的文件。
- 材料不足或需要扩大范围时通过 Orca 提问，不自行扩展。
- 不做无关重构、格式化、功能扩展、提交、推送、发布、依赖安装或外部系统操作。
- 不运行 `run-create`、`run-use`、`task-create`、`task-update` 或协调器的
  `check --wait`，不绑定或接管 Run；生命周期只按 Orca 注入的 worker 前导信息执行。

## 2. 检查要求

- 使用当前终端能正确解析的路径和命令；不要照搬不兼容当前 Shell 的路径写法。
- 只运行派工要求的检查；同一检查通过后不为形式重复运行。
- 区分自动化验证、实机验证、代码审查和未验证，不把代码推断表述为实机通过。

## 3. 返回要求

完成时按 Orca 注入的生命周期要求发送且只发送一次有效 `worker_done`，并如实报告：

- 实际读取和修改的文件；
- 完成内容与检查结果；
- 按严重程度排列的发现；
- 未验证内容和遗留风险。

只读审查的完成信号只代表审查结束。不要修改 `docs/pm/PLAN.md`，也不要替 Codex 写最终验收。
若完成信号被 Orca 拒绝，不要尝试运行协调器命令恢复；等待 Codex 停止并重派。
