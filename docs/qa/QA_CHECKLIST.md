# QA_CHECKLIST.md — 长期核心回归基线

> 基线由 MVP 验收（`docs/pm/PLAN.md` A1–A6）沉淀，并经 **2026-08-17 neat-freak 复审**：
> 下列 A1–A4 已对照 `src/family-insurance-dashboard.html` **静态验证通过**（含 A1 模板 SHA256 复算）。
> QA 在真实浏览器测试中持续补充高价值回归项；neat-freak 负责修剪、去重与更新。

## 导出与导入链路（已验证）
- [x] **A1** 导出 Excel 模板下载的 `.xls` 与代码内嵌源文件字节一致
      （文件名 `家庭保单明细表模板.xls`；5417 bytes；SHA256 `B60855A242E1ECD23199D88ABDD29662025FCAD164A26AF8B2701C9B6ED53146`）
- [x] **A2** 填写模板导入：受支持列映射正确，原样示例 / 占位行跳过，明细与汇总立即刷新
- [x] **A3** 添加保单后明细立即显示，`localStorage` 持久化，筛选项无重复

## 响应式（已验证）
- [x] **A4** 宽屏（1920px）充分利用空间；窄屏（390px）无页面级横向溢出，KPI 单列
      （代码中无 1280px 固定上限；容器用 `max-width` + `minmax` 栅格）

## 数据与安全
- [x] 真实保单数据仅存本机浏览器，无联网上传（纯前端、无 fetch / 外链）
- [ ] 合同附件 `IndexedDB` 上传 / 打开 / 替换 / 删除（需 QA 实机验证各操作）

## MVP 代码审查沉淀的回归项（来自 PLAN.md 审查发现处置，均已采纳）
- [x] 导入时识别并跳过原样示例 / 占位行，不导入为真实保单
- [x] 刷新后筛选项不重复追加（`initFilters` 重建选项并保留仍有效的当前筛选值）
- [x] 文件选择器 `accept` 不含 `.xlsx`（仅 `.json` / `.csv` / `.xls`）
- [x] 保险期间可计算时以日期状态为准；模板不含保险期间时仍采用导入状态
- [x] Excel 导出仅提供“模板”下载；保单数据备份走 JSON 导出 / 导入（**无「导出当前数据 xls」功能**）

## 待补充
- （由 QA 在后续真实浏览器测试中持续添加高价值回归项，如：列拖拽排序、双击单元格编辑、
  批量同名同步、费率趋势 YoY 计算、到期提醒与桌面通知等）
