# BUGS.md

> Bug 由 QA 在测试中发现并写入，按严重程度分级。

| ID | 严重程度 | 描述 | 状态 | 处置 |
|---|---|---|---|---|
| BUG-20260907-01 | P0 | 云非空时登录覆盖本机草稿：`store.loadAll` 云端分支在云有数据时直接用云行替换本机 `policies` 并写回 localStorage；首登保护只覆盖“云为空”情形。用户 8 条变 6 条疑似此路径（代码路径已核对；用户决定不再追查恢复）。预防：先「加密备份」再登录/导入。根治方向：loadAll 改为合并（云行 + 无 `_serverId` 本机草稿并存，草稿标待上传）。 | OPEN（用户接受暂缓） | 待排期；回归项已进 QA_CHECKLIST |

> 历史：2026-09-07 前无未关闭 Bug（见 CODE_REVIEW.md 历史节）。
